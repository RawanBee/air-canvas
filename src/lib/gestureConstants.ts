/** Pinch must hold this many camera frames before draw starts. */
export const GESTURE_ENTER_FRAMES = 3;
/** Pinch must read “open” this many frames before draw ends (reduces line breaks). */
export const GESTURE_EXIT_FRAMES = 8;
/** Missing hand landmarks for this many camera frames before we end the stroke. */
export const HAND_LOST_GRACE_CAMERA_FRAMES = 8;
